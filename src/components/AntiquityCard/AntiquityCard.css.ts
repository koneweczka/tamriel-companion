import { style } from '@vanilla-extract/css';

export default {
  card: style({
    display: 'block',
    padding: 16,
    minHeight: 40,
    width: 'fit-content',
    minWidth: 120,
    alignItems: 'center',
    textAlign: 'center',
  }),
};
